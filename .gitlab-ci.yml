image: node:22

pages:
  script:
  - npm install
  - npm run build
  - mv ./bundle ./public
  - cp --recursive ./website/* ./public/

  artifacts:
    paths:
    - lib
    - public

  only:
  - master
